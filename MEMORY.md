Fixed two lint errors: 1) basePath.ts line 4 - changed string concatenation "/" + path to template literal `/${path}`; 2) Starburst.tsx line 49 - added `<title>Starburst decorative element</title>` inside the SVG to fix the empty title accessibility error. The `data-testid="starburst"` was already on the outer div per spec.
